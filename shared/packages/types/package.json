{
  "name": "@travel-health/types",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "tsup src/index.ts --watch --format esm,cjs --dts",
    "build": "tsup src/index.ts --format esm,cjs --dts",
    "typecheck": "tsc --noEmit",
    "lint": "echo \"No lint configured for @travel-health/types\"",
    "test": "echo \"No tests yet for @travel-health/types\""
  },
  "devDependencies": {
    "tsup": "^8.2.4",
    "typescript": "^5.8.3"
  }
}
