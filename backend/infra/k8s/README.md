# Kubernetes Placeholder

This directory is reserved for service deployment manifests (phase 2+).

Planned files:

- Namespace and config maps
- API gateway deployment/service
- Service deployments for travel, health, scraper, analyzer, score-api
- RabbitMQ/Redis/Postgres operators or managed service references
